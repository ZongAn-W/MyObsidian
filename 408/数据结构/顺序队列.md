- 和顺序栈相类似，除了用一组地址连续的存储单元存放元素之外，还需附设两个指针front和rear分别指示队列头元素以及队列尾元素的位置

```cpp
#define MaxSize 10 // 定义队列元素的最大个数

typedef struct {
    ElemType data[MaxSize]; // 存放队列元素
    int front; // 队头指针
    int rear; // 队尾指针
} SqQueue;
```

[[顺序队列的基本操作]]
[[循环队列]]