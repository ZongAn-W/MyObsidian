- 根据指定的位置序号 i ，获取顺序表第 i 个数据元素的值
- 由于顺序存储具有随机存取的特点，可直接通过数组下标得到 $elem[i-1]$ 单元存储第 i 个数据元素
```c
Status GetElem(SqList L, int i, ElemType &e){
    if(i<1 || i>L.length) return ERROR; // 判断 i 值是否合理，若不合理，返回 ERROR
    e=L.data[i-1]; // data[i-1] 单元存储第 i 个数据元素
    return OK;
}
```