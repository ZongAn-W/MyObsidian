【总结】栈是受限线性表，还是线性表，所以存储方式基本相同，操作步骤上有些不同。




• 顺序栈：利用顺序存储结构实现的栈，即利用一组地址连续的存储单元依次存放自栈底到栈顶的数据元素，同时附设指针 top 指示栈顶元素的位置即数组下标。
```c
#define MaxSize 10 // 定义栈中元素的最大个数

typedef struct{
    Elemtype data[MaxSize]; // 存放栈中元素
    int top; // 栈顶指针
} SqStack;