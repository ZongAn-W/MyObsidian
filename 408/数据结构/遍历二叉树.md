## 层次遍历
- 从第一层根节点开始，依次每行从左到右遍历
- 设置一个队列，依次让每个结点入队并出队访问
- 先访问根结点，然后依次访问其左、右孩子，再从左往右依次访问左、右孩子的左、右孩子。

1. 初始化一个辅助队列，先将二叉树根结点入队；
2. 若队列非空，则队头结点出队，访问出队结点，若有左、右子树，则将其左、右子树的根结点依次插入队尾；
3. 反复执行（2），直至队列为空。

```text
定义并初始化一个队列；
将根结点入队；
循环判断（队是否为空）{
    队头结点出队；
    操作队头结点，并得到其左右孩子；
    如果有左右孩子则依次入队；
}
```

```cpp
// 二叉树结点
typedef struct BiTNode {
    ElemType data;
    struct BiTNode *lchild, *rchild;
} BiTNode, *BiTree;

// 链式队列结点
typedef struct LinkNode {
    BiTNode *data;
    struct LinkNode *next;
} LinkNode;

// 链式队列
typedef struct {
    LinkNode *front, *rear; // 队头、队尾
} LinkQueue;

void LevelOrder(BiTree T) {
    LinkQueue Q;
    InitQueue(Q); // 初始化辅助队列
    BiTree p; // 定义一个指针来接收出队结点
    EnQueue(Q, T); // 将根结点入队
    while (!IsEmpty(Q)) { // 队列不空则循环
        DeQueue(Q, p); // 队头结点出队
        visit(p); // 访问出队结点，访问操作可自行定义
        if (p->lchild != NULL)
            EnQueue(Q, p->lchild); // 左子树不空，则左子树根结点入队
        if (p->rchild != NULL)
            EnQueue(Q, p->rchild); // 右子树不空，则右子树根结点入队
    }
}
```

## 先中后序遍历

- **先序遍历**：先遍历根结点，再先序遍历左子树，最后先序遍历右子树；
- **中序遍历**：先中序遍历左子树，再访问根结点，最后中序遍历右子树；
- **后序遍历**：先后序遍历左子树，再后序遍历右子树，最后访问根结点。

【总结】先序（根左右）、中序（左根右）、后序（左右根）。

- **先序遍历【算法思想】**
若二叉树为空，则空操作；否则：（1）访问根结点；（2）先序遍历左子树；（3）先序遍历右子树。

```cpp
void PreOrder(BiTree T) {
    if (T != NULL) {
        visit(T); // 访问根结点
        PreOrder(T->lchild); // 递归遍历左子树
        PreOrder(T->rchild); // 递归遍历右子树
    }
}
```

- **中序遍历【算法思想】**
若二叉树为空，则空操作；否则：（1）中序遍历左子树；（2）访问根结点；（3）中序遍历右子树。

```cpp
void InOrder(BiTree T) {
    if (T != NULL) {
        InOrder(T->lchild); // 递归遍历左子树
        visit(T); // 访问根结点
        InOrder(T->rchild); // 递归遍历右子树
    }
}
```

- **后序遍历【算法思想】**
若二叉树为空，则空操作；否则：（1）后序遍历左子树；（2）后序遍历右子树；（3）访问根结点。

```cpp
void PostOrder(BiTree T) {
    if (T != NULL) {
        PostOrder(T->lchild); // 递归遍历左子树
        PostOrder(T->rchild); // 递归遍历右子树
        visit(T); // 访问根结点
    }
}
```

### 【总结】

1. 三种遍历算法中，递归遍历左、右子树的顺序都是固定的，只是访问根结点的顺序不同。
2. 不管采用哪种遍历算法，每个结点都访问一次且仅访问一次，故时间复杂度都是 $O(n)$。
3. 在递归遍历中，递归工作栈的栈深恰好为树的深度，即空间复杂度为 $O(h)$。所以在最坏情况下，二叉树是有 $n$ 个结点且深度为 $n$ 的单支树，遍历算法的空间复杂度为 $O(n)$。

## 遍历序列确定二叉树
- 单独给出某一遍历序列（先序、中序、后序、层次）并不能唯一确定一棵二叉树。
- 如果二叉树具有唯一的先序遍历序列，但是一个先序遍历序列并不能唯一确定一个二叉树形态，可能对应多种二叉树形态。
- 由二叉树的**先序序列和中序序列**、**后序序列和中序序列**，或**层序序列和中序序列**，均能唯一地确定一棵二叉树。
- 三种方式主要都是确定树的根节点，并根据中序序列划分左右子树，再找到左右子树的根节点，以此类推
