- 将表的第 i 个元素删去
- 将元素向前移动来补空位

```c
Status ListDelete(SqList &L, int i){
    if(i<1 || i>L.length) return ERROR; // i 值不合法
    for(int j=i; j<=L.length-1; j++) // 需要将第 i+1 到第 n 个元素前移，即在数组下标为 i~n-1 的元素
        L.data[j-1]=L.data[j]; // 被删除元素之后的元素前移
    L.length--; // 表长减 1
    return OK;
}
```