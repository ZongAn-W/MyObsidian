- **双端队列：** 指允许两端都可以进行入队和出队操作的队列。将队列的两端分别称为前端和后端，两端都可以入队和出队。

【注意】若只使用其中一端进行插入、删除操作，则等同于栈。若使用一端插入，使用另一端删除，则等同于队列。

→ 所以栈能实现的功能，双端队列肯定能实现。

→ 双端队列还能引入两种变种双端队列：

- **输入受限的双端队列：** 只允许从一端插入、两端删除的线性表。
- **输出受限的双端队列：** 只允许从两端插入、一端删除的线性表。