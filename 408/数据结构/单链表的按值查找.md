- 根据给定值来查找表中与之相同的数据
```c
LNode *LocateElem(LinkList L, ElemType e){ // 在带头结点的单链表 L 中查找值为 e 的元素
    LNode *p=L->next; // 初始化，p 指向首元结点
    while(p!=NULL && p->data!=e) // 顺链域向后扫描，直到 p 为空或 p 所指结点数据等于 e
        p=p->next; // p 指向下一个结点
    return p; // 查找成功返回值为 e 的结点地址 p，查找失败 p 为 NULL
}