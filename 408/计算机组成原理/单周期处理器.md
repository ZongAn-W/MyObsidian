- 对所有的指令都选用相同的执行时间完成
- 每条指令都在一个时钟周期完成
- 指令串行执行
- 指令周期取决于执行时间最长的指令的执行时间；==处理器时钟频率较低==
- 对本能更快完成的指令，仍需再较长周期完成，会降低整个系统的运行速度
- [[CPI]]为1
- 要把一条指令设计的控制信号一下子全发出去，在指令执行过程中控制信号不变

- 不能使用[[单总线结构的数据通路]]    （ [[数据通路的基本结构]]）
- 可以使用[[专用结构的数据通路]]