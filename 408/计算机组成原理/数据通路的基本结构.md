## 总线式数据通路
将通用寄存器、ALU和内部寄存器等主要部件连接至一条或多条==CPU内部总线==
- **[[单总线结构的数据通路]]**：所有数据传输共享一条内部总线，冲突多，性能低
- **多总线结构**：提供多条独立总线，支持更高并发度，效率高
【注意】==片内总线==指的是CPU内部数据流转共享的通路，==系统总线==指的是计算机各大部件共享的通路
## 专用数据通路方式
- 采用的是点对点专用连线连接寄存器、ALU等部件
- 各数据线能并行传输，避免总线冲突
- 优点：效率高、延迟低
- 缺点：复杂、扩展性差、成本高