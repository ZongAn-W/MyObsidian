![[变址寻址.jpg|390]]

- 操作数的内存地址由CPU内变址寄存器的内容和指令中形式地址A求和得到
- EA=(IX)+A
- IX可以是专用变址寄存器或指定的通用寄存器，==一般可面向用户==，其内容作为偏移量，在程序中由用户动态修改，形式地址A作为基地址不变
- 优点：可扩大寻址范围（IX的位数大于A的位数）；在数组处理过程中，可设定A为数组的首地址，通过不断修改IX，便可形成数组中任意数据的地址。适合编制循环程序
- 缺点：操作数需要计算得到

