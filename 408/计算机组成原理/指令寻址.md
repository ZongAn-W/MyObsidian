## 顺序寻址
- 一般情况下，执行一条指令后，会顺序执行下一条指令，PC里的指令地址会自动加1
- 这里的加1指的是下一条指令，如果一条指令占两个地址，则下一条指令地址实际加2
## 跳跃寻址
- 因为判断、函数调用，不继续顺序执行，反而跳转到别的地址执行
- 通过转移类指令实现，转移目标地址由指令给出
- **绝对转移**：地址码直接给出目标地址
- **相对转移**：地址码给出相对于当前PC值的偏移量