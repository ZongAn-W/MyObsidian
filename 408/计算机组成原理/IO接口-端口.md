## I/O接口
### I/O端口

> [!note] 背景
> CPU想要读取某个寄存器内容时，如何进行指定？毕竟接口中可能存在很多的寄存器。

#### I/O端口
指接口电路中可以被CPU直接寻址访问的寄存器，若干个端口加上相应的控制逻辑电路组成接口。
- 一般主要包括数据端口（支持CPU读写）、状态端口（仅支持读）、控制端口（仅支持写）。

> [!warning] 注意
> 端口和接口是不同的概念。实际上CPU对接口的访问就是访问接口中的寄存器。
