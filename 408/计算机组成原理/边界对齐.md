- 若按字节编址，要求数据存储地址是自身大小的整数倍
- 字节可位于任意地址，半字地址一定是2的整数倍，字地址一定是4的整数倍![[边界对齐.jpg]]
- 若按边界对齐方式存储，CPU可通过一次访存读取完整数据（左图），反之，若数据跨越两个存储单元，则需要两次访存，效率低（右图）
- 为了满足对齐要求，可必要时填充空白字节，拿空间换时间

C语言的sturct类型中边界对齐有两个要求：
- 每个成员按其类型的大小对齐：char是1字节；short是2字节；int是4字节
- struct的长度必须是最大对齐值的整数倍