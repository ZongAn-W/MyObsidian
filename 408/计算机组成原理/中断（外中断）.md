- 来自CPU外部，和CPU执行指令无关的事件引起的中断，一般可分为可屏蔽中断和不可屏蔽中断
【注意】外部I/O设备通过特定的中断请求信号线向CPU提出中断请求，CPU每执行完一条指令就检查中断请求信号线，若检测到中断请求，则进入中断响应
### 可屏蔽中断
- 通过可屏蔽中断请求线INTR向CPU发出中断请求
- 可通过设置屏蔽字来屏蔽从而不被处理
### 不可屏蔽中断
- 通过专门的不可屏蔽中断线NMI向CPU发出请求
- 一般是紧急的硬件故障如掉电等