![[后退N帧协议.png]]
- 发送窗口>1 接收窗口=1
- 接收方连续收到多个数据帧的时候，可以仅仅返回最后一个帧的ACK
- 关于超时重传 若发送方超时未收到ACK_i，则重传i号帧以及后面的所有帧

数据帧丢失情况：

![[后退N帧的数据帧丢失情况.png]]
- 若i帧超时，需要重传i号帧及其后续的所有帧
- 后退并不是指滑动窗口后退，而是在超时的时候把已经发送的帧重传
确认帧丢失的情况
![[GBN的确认帧丢失情况.png]]

如果不满足$W_T+W_R \le 2^n$   n是给帧编号的比特数：
- 接收方无法辨别新帧和旧帧

