![[停止等待协议.png]]
- SW的发送窗口和接收窗口大小都为1；只需要用1bit给帧进行编号

==数据帧丢失的情况==：
- 发送方在发送数据帧的时候也会设置一个计时器，如果计时器超时，会重传这个帧，重传数据帧的时候重置计时器
==确认帧丢失的情况==：
![[SW确认帧丢失的情况.png]]
- 如果没有帧序号，接收方无法判断重复帧
==数据帧有差错的情况==：
- 接收方收到数据帧，单检测出差错，将此帧丢弃，且不返回ACK