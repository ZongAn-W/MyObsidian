- 是在不可靠的IP层之上实现可靠的数据传输协议
- 解决传输的可靠、有序、不丢失、不重复问题
主要特点：
1. TCP是面向连接的传输层协议，TCP连接是一条逻辑连接
2. 只能有两个端点，是端到端的
3. 提供可靠交付的服务
4. 提供全双工通信；TCP的两端设有发射缓存和接受缓存
		发送缓存存放：1.准备发送的；2.已发送还没收到确认的
		接受缓存存放：1.按序到达还尚未被读取的；2.不按序到达的
5. TCP是面向字节流的