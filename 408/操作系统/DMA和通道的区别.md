- DMA(Direct Memory Access，直接内存访问）是一种让外部设备不经过CPU，直接与内存进行数据传输的硬件技术，核心目的是减轻CPU在数据传输中的负担，提高系统效率。是一种简单的硬件控制机制，仅能完成"设备与内存之间直接的数据传输"这一单一任务，相当于CPU的"专用数据搬运工"。
- 通道是一种小型专用处理器（I/O处理器），拥有自己的指令集（通道指令）和程序（通道程序），能独立执行一系列I/O控制操作，相当于"专门管理I/O的副CPU"。

DMA：适用于数据传输路径固定、传输过程简单的场景。
通道：适用于设备类型多、I/O操作复杂的场景，尤其在大型计算机系统中。