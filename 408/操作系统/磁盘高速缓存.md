- 为了解决CPU与I/O速度不匹配的矛盾，需要提高CPU与I/O设备的并行运行程度。
- ==磁盘高速缓存技术是指利用内存中的存储空间来暂存从磁盘中复制的一系列盘块中的信息。==

==磁盘高速缓存逻辑上属于磁盘，物理上则是驻留在内存中的盘块==。其在内存分配的形式有两种：
- 在内存中开辟一个单独的存储空间作为磁盘高速缓存，大小固定；
- 把未利用的内存空间作为一个缓冲池，供请求分页系统和磁盘I/O共享。