# 人工操作方式
- 用户将自己编写的程序通过纸带、卡片等输入设备送入计算机内存，然后启动程序运行
- 弊端：用户独占全机；CPU等待人工操作；浪费资源；对用户专业要求高
# 脱机I/O方式
- 引入了专门的外围计算机来处理输入输出任务，实现了输入输出操作和CPU运算的分离
- 在脱机IO方式下，用户首先将程序和数据通过外围计算机预先录入到磁带等存储介质上，这个过程被称为脱机输入，当主机需要运行程序的时候，直接从磁带上快速读取程序和数据到内存中；输出的过程和输入一样，也有一个磁带和外围计算机，被称为脱机输出
- 提高了I/O速度
- 减少了CPU的空闲时间