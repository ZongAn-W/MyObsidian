在设备管理子系统中，引入缓冲区的主要目的：
- 缓和CPU与I/O设备间速度不匹配的矛盾。
- 减少对CPU的中断频率，放宽对CPU中断响应时间的限制。
- 解决基本数据单元大小（即数据块大小）不匹配的问题。
- 提高CPU和I/O设备之间的并行性。

实现缓冲区的方法：
- 采用硬件缓冲区，成本高，除特殊用途外使用极少。
- 在内存划出一块存储区，专门用来临时存放输入／输出数据，根据系统设置的缓冲区个数，缓冲技术可以分为[[单缓冲]]、[[双缓冲]]、[[循环缓冲]]和[[缓冲池]]四种。