增加约束：

```sql
ALTER TABLE <表名>
ADD CONSTRAINT <约束名> <约束条件>;
```

删除约束：

```sql
ALTER TABLE <表名>
DROP CONSTRAINT <约束名>;
```

例子：

```sql
ALTER TABLE Student
DROP CONSTRAINT C1;
```

```sql
ALTER TABLE Student
ADD CONSTRAINT C1 CHECK (Sno BETWEEN '10000000' AND '29999999');
```