关系数据语言可以分为三类：

1. 关系代数语言：用关系运算表达查询，如 ISBL。
2. 关系演算语言：用谓词表达查询。
3. 结构化查询语言：SQL，兼具关系代数和关系演算特点。

关系演算又可分为：

- 元组关系演算，如 ALPHA、QUEL。
- 域关系演算，如 QBE。

SQL 的特点：

- 不仅有查询功能。
- 还包含数据定义、数据操纵、数据控制等功能。
- 是集 DQL、DDL、DML、DCL 于一体的关系数据库语言。
- 从 20 世纪 80 年代起成为关系数据库标准语言。