数据加密是保护数据安全的重要技术，尤其适用于高度敏感数据。

加密基本思想：

```text
明文 → 加密算法 → 密文
```

不知道解密方法或密钥的人无法直接获得数据内容。

### 11.1 存储加密

存储加密保护数据在磁盘上的安全。

常见方式：

- 透明存储加密：由 DBMS 自动加密和解密，对用户和应用程序透明。
- 非透明存储加密：通过加密函数实现，应用需要显式处理加密和解密。

### 11.2 传输加密

传输加密保护数据在网络传输过程中的安全。

常见方式：

- 链路加密：对[[数据链路层]]传输数据加密，通常报文和报头都加密。
- 端到端加密：在发送端加密、接收端解密，通常只加密报文内容。

数据库系统中常见的传输保护方案可以基于 SSL 等安全协议。