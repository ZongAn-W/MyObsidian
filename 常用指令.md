- 汇编指令通常分为数据传送指令、算术和逻辑运算指令、控制流指令
- $<reg>$：表示==任意寄存器==，若其后带有字数、则指定其位数，如$<reg32>$表示32位寄存器
- $<mem>$:表示==内存地址==（如[eax]、dword ptr [eax+cbx]）
- 读写内存地址时，可通过前缀表明读写此地址上的数据长度。dword ptr双字，word ptr单字，byte字节等
- $<con>$:表示8位、16位或者32位==常数==。$<con8>$表示8位常数