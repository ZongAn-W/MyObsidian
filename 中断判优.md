【问题】在主程序执行时，同时来到多个中断，如何选择一个中断进行响应
- 响应中断前一个设计好中断响应的优先级，根据优先级设置通过==硬件排队器==实现，或通过==软件查询程序==实现
【总结】
在响应优先级设置上：
- 硬件故障属于最高级中断，其次是软件中断
- 非屏蔽中断高于可屏蔽中断
- 高速设备高于低速设备
- 输入设备高于输出设备
- 实时设备高于普通设备
【注意】
需要分清的是==中断响应优先级==是固定决定的，不可动态修改，而==中断处理优先级==是通过设置屏蔽字来动态调整，指的是在中断处理过程中是否能被别的中断所中断

[[中断响应优先级和处理优先级的例题]]

