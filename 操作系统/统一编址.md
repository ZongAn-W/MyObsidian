- 将I/O端口当作内存单元来对待，把I/O端口的地址直接分配在内存地址空间中，二者共享同一个地址空间。
- 关键特点：无需专用I/O指令，使用普通的内存访问指令即可访问I/O端口。硬件上不区分内存和I/O的控制信号，CPU访问端口和访问内存的操作时序一致。占用部分内存地址空间，会减少可寻址的内存容量。
- 优势：可直接使用丰富的内存操作指令处理I/O数据，编程灵活；硬件设计更简单，无需额外的I/O控制逻辑。
- 不足：内存地址空间被占用；程序中难以直观区分内存操作和I/O操作，可读性稍差。