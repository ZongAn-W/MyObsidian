- 通过路径名间接引用目标文件
- 核心原理：软链接本身是一个独立的特殊文件，他的内容并不包含目标文件的数据，而是存储着目标文件的绝对路径或相对路径
### 关键特点
- 独立的文件属性：软链接是一个单独文件，有自己的文件、inode、大小、权限
- 不依赖目标文件的inode
- 支持跨文件系统和目录
- 不影响目标文件的链接计数
### 存在的问题
- 共享信息出错
- 多次读盘
- 磁盘开销大