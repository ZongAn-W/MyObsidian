### 抖动
- 刚刚换出的页面马上又要换入主存，刚刚换入的页面马上又要换出主存——抖动
- 一个进程在页面对换上花的时间超过执行时间说明该进程处于抖动状态
- 主要原因：同时运行的进程数过多，进程频繁访问的页面数高数可用的物理块数
### 工作集
- 降低缺页率、防止抖动的内存管理方法
- 在某段时间间隔以内，进程实际所要访问页面的集合
- 核心思想：用进程在过去某段时间内的行为来作为进程在将来某段时间内行为的近似
- 工作集的窗口尺寸太小不能表示进程的局部特征
- 工作集的窗口尺寸太大失去了工作集的意义
- 根据作业的最大工作集在估算其所需的物理块数量