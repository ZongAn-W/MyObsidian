- 页是信息的物理单位，分页是由于系统管理的需要，而不是用户的需要
- 段是信息的逻辑单位，分段是为了能更好满足用户的需要
- 页的大小固定由系统决定，段的长度不固定，取决于用户编写的程序
- 在一个进程中，段表只有一个，而页表可能有多个
- 分页的作业地址空间是==一维==的——用户只需要一个单一的地址值就能定位任意内存单元
- 分段的作业地址空间则是==二维==的——访问内存必须明确段号和段内偏移量
- [[分页存储管理]]  [[分段存储管理]]