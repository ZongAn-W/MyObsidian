- 把OS按照功能划分为若干个模块、每个模块完成一个特定的功能；如进程管理、内存管理、文件系统、设备驱动等
- 这些模块具有相对独立性
- 优点：提高了OS设计的正确性、可理解性、易维护性，增强了OS的可适应性，加速OS开发过程
- 缺点：==模块间的接口规定很难满足对接口的实际需求==；OS 设计阶段，设计者必须做出一系列决定，每个决定必须建立在上一个决定的基础上。但各模块设计者齐头并进，每个决定无法建立在上一个已验证的正确决定的基础上，因此==无法找到一个可靠的决定顺序==。该方法又称为无顺序模块法。