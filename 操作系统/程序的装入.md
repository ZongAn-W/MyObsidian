- ==绝对装入方式==：完全按照目标程序中所给定的地址装入内存，目标程序中使用的是绝对地址
- ==可重定位装入方式==：又称静态重定位，用户的目标程序地址都是从0开始的，将装入模块装入到内存某个位置时，根据目前内存使用情况进行地址转换
- ==动态运行时的装入方式==：又称动态重定位，在把装入模块装入内存后，将地址转换推迟到程序真正要执行时才进行。在程序执行过程中，每次要访问内存时动态转换地址，依赖重定位寄存器记录程序在内存的起始地址。程序在内存中若发生移动，需要这种装入方式

