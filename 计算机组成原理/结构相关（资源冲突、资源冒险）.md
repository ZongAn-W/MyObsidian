- 不同资源在同一时刻争用同一功能部件所引发的冲突，受限于硬件资源的物理限制
解决方案：
- 1.前指令访存时，使后一条相关指令（以及其后续指令）暂停一个时钟周期
- 2.确保每个功能部件在每条指令中至多只用一次，且总在固定阶段使用
- 3.可以单独设置数据存储器和指令存储器，使两项操作各自在不同的存储器中进行，这属于资源重复配置，如将Cache分成数据Cache和指令Cache，指令和数据分隔在不同的部件中