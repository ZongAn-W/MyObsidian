- 当流水线遇到转移指令或其他改变PC信号的指令而造成断流时，会引起控制相关
- 程序调用、中断、异常的发生都可能引起流水线的阻塞
- 例有可能在第一条指令的第四个阶段才判断出需要跳转，则顺序读入的指令就会不符合程序的逻辑
解决方案：
- 延迟分支处理：对于由分支指令引起的冲突，可由软件在分支指令后插入若干空指令，或由硬件自动阻塞（插入气泡）
- 分支预测技术：对转移指令进行分支预测，尽早生成转移目标地址分支；静态分支：固定预测转移发生或不发生；动态预测：根据转移历史动态调整预测策略，准确率更高；若预测错误，则需清空已进入流水线的错误路径指令，并从正确目标地址重新取指