- 后面指令需要用到前面指令的结果，但是前面指令还没产生
解决方案：
- 1.延迟执行相关指令：把遇到数据相关的指令以及后续的指令都暂停一至几个时钟周期，直到数据相关问题消失后再继续执行；可分为硬件阻塞和软件插入空操作指令两种方法
- 2.==数据旁路技术==：设置相关的转发通路，不等前一条指令把计算结果写回寄存器组，下一条指令也不再读寄存器组，直接把前条指令的ALU的计算结果作为自己的输入数据开始计算过程
- 3.编译优化：通过编译器调整指令顺序来解决数据相关