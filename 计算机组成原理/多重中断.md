# 多重中断（中断嵌套）
- 在执行中断服务程序的过程中，若又出现了新的优先级更高的中断请求。CPU暂停现行的中断服务程序，转去处理新的中断请求

多重中断的条件：
- 在中断服务程序中提前设置开中断指令
- 优先级别高的中断有权中断优先级别低的中断

中断屏蔽技术：
- 每个中断源都有一个屏蔽触发器，1表示屏蔽该中断源的中断请求。0表示接受该中断源的中断请求；所有屏蔽触发器组合在一起构成了一个屏蔽字寄存器，其中的内容称为屏蔽字。
- 某个中断源中的屏蔽字中的1越多，意味能屏蔽的中断越多，优先级越高
- 每个屏蔽字中至少有一个1，屏蔽自身中断

[[中断响应优先级和处理优先级的例题]]