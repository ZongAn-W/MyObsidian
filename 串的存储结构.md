• 定长顺序存储：用静态数组实现，这种方式存储串长度是有限制的。

```c
#define MAXLEN 255  // 预定义最大串长度为 255

typedef struct {
    char ch[MAXLEN];  // 每个分量存储一个字符
    int length;       // 串的实际长度
} SString;
```

• 堆分配存储：动态数组实现。

```c
typedef struct {
    char *ch;  // 按串长分配存储区，ch 指向串的基地址
    int length;  // 串的长度
} HString;

HString S;

S.ch = (char *)malloc(MAXLEN * sizeof(char));
```


• 链式存储

```c
typedef struct StringNode {
    char ch;  // 每个结点存储 1 个字符
    struct StringNode *next;
} StringNode, *String;
````

```
typedef struct StringNode {
    char ch[4];  // 每个结点存储多个字符
    struct StringNode *next;
} StringNode, *String;
```
